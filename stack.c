#include "monty.h"
/**
 * f_stack - prints the top element of the stack
 * @head: stack head
 * @counter: line number
 * Return: no return value
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
